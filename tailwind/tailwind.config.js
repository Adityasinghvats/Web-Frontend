/** @type {import('tailwindcss').Config} */
module.exports = {
  // This means inside main directory there is a src folder with the files of extension
  // .html and .js it will target them directly
  content: ["./src/**/*.{html,js}"],
  theme: {
    extend: {},
  },
  plugins: [],
}

